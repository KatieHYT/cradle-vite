# Cradle

## Usage
- Step-1:  
  Clone this repository and `cd` to `cradle-vite`.
  
- Step-2:  
  Install dependency
  ```
  npm install
  ```
- Step-3:
  Run the service
  ```
  npm run dev -- --port CUSTOM_PORT
  ```

